# إصلاح مشاكل قاعدة البيانات في نظام المحاسبة

## المشكلة

تواجه المشاكل التالية في النظام:
- خطأ "permission denied for table" (رمز الخطأ 42501) عند محاولة الوصول إلى الجداول
- وظيفة RPC غير موجودة: `create_required_tables`
- جداول قاعدة البيانات إما غير موجودة أو غير مكونة بشكل صحيح

## الحل

### الخطوة 1: قم بتسجيل الدخول إلى لوحة تحكم Supabase

1. توجه إلى [لوحة تحكم Supabase](https://app.supabase.com/)
2. قم بتسجيل الدخول واختر المشروع المرتبط بتطبيقك

### الخطوة 2: تنفيذ ملف SQL لتهيئة قاعدة البيانات

1. في لوحة التحكم Supabase، انتقل إلى قسم "SQL Editor"
2. انسخ محتويات ملف `database_initialization.sql` وألصقه في محرر SQL
3. انقر على "Run" لتنفيذ الاستعلام
4. هذا سينشئ وظيفة RPC `create_required_tables` ويضبط أذونات الأمان

### الخطوة 3: التحقق من إنشاء الجداول والسياسات

1. في لوحة التحكم Supabase، انتقل إلى قسم "Table Editor"
2. تحقق من وجود الجداول التالية:
   - categories
   - transactions
   - accounts
   - budgets
   - user_settings

3. ثم انتقل إلى قسم "Authentication" ثم "Policies"
4. تأكد من وجود سياسات RLS لكل جدول تم إنشاؤه

### الخطوة 4: استخدام التطبيق

1. عد إلى التطبيق وقم بتسجيل الدخول
2. اضغط على زر "تهيئة قاعدة البيانات" إذا ظهرت شاشة الخطأ
3. بمجرد اكتمال التهيئة، سترى لوحة التحكم تعمل بشكل صحيح

## ملاحظات إضافية

- تأكد من أن مستخدم التطبيق قد تم تسجيله في Supabase أولاً
- السبب الرئيسي للخطأ هو عدم وجود سياسات RLS (Row Level Security) التي تسمح للمستخدمين بالوصول إلى بياناتهم الخاصة
- ملف `database_initialization.sql` يقوم بإنشاء:
  1. الجداول المطلوبة مع الحقول الصحيحة
  2. سياسات RLS التي تسمح للمستخدمين بالوصول إلى بياناتهم فقط
  3. وظيفة RPC التي يستخدمها التطبيق للتحقق من وجود الجداول وإنشائها عند الحاجة

- في حالة استمرار المشكلة، قد تحتاج إلى إعادة تعيين كلمة مرور المستخدم في Supabase أو التحقق من أن دور المستخدم لديه الأذونات المناسبة 